import operator

# possible operators map, to be used with size
OPERATORS_MAP = {"=": operator.eq,
                 ">": operator.gt,
                 "<": operator.lt,
                 "<=": operator.le,
                 ">=": operator.ge,
                 }
